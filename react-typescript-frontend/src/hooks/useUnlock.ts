import { useEffect } from 'react';
import { JsonObject } from 'react-use-websocket/dist/lib/types';

const useUnlock = (message: JsonObject) => {
    useEffect(() => {
        
    }, []);
};

export default useUnlock;