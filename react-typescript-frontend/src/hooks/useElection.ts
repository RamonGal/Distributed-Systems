import { useEffect } from 'react';
import { JsonObject } from 'react-use-websocket/dist/lib/types';

const useElection = (message: JsonObject) => {
    useEffect(() => {
        
    }, []);
};

export default useElection;